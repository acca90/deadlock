const Deadlock = (function () {
    /**
     * Inicializa simulação de deadlock
     */
    const init = function () {
        document.getElementById('html').innerHTML = "it's work";
    };
    /**
     * Métodos públicos
     */
    return {
        init: init
    };
})();